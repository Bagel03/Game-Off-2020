

namespace `game.views`(
    @tag("credits");
    class Credits extends WebComponent{
        constructor(){
            super()
        }
        async onConnected(){
            await super.onConnected();

        }
    }
);